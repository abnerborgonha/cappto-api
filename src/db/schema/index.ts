import { companies } from '@/db/schema/companies.ts';
import { contacts } from '@/db/schema/contacts.ts';
import { providers } from '@/db/schema/providers.ts';

export const schema = {
  companies,
  contacts,
  providers
}